## 2026-05-17 　GuardSync プロジェクト計画書 初版作成（v1.0）

### 変更内容
- `20260518_GuardSync_プロジェクト計画書.docx` を新規作成（7セクション）
- `20260518_GuardSync_プロジェクト計画書.xlsx` を新規作成（5シート：概要/WBS・タスク/スケジュール・マイルストーン/リスク管理/ToDo・事前タスク）
- `20260518_GuardSync_プロジェクト計画書.pptx` を新規作成（10スライド）

### 変更理由
- komainu.cloudを超える警備会社向けSaaS「GuardSync」のプロジェクト計画書を作成するため
- MINプラン（2026-06-20リリース）・MAXプランの要件定義・WBS・リスク管理を文書化

### 影響範囲
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.docx`（新規）
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.xlsx`（新規）
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.pptx`（新規）

## 2026-05-17 　GuardSync プロジェクト計画書 v1.1更新（MAXプラン追加）

### 変更内容
- Word: MAXプラン追加要件セクション追加（SaaS販売・FC展開・契約違反監視・優先人員融通）
- Excel: 「MAXプラン追加要件」「MAXプラン全体構成」シートを追加
- PowerPoint: スライド11〜13追加（SaaS販売・FC/フランチャイズ・スーパー管理者）

### 変更理由
- MAXプランに他社販売・FC展開・契約違反監視（スーパー管理者機能）・優先人員融通を追加するため

### 影響範囲
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.docx`
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.xlsx`
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.pptx`

## 2026-05-17 　GuardSync プロジェクト計画書 v1.2更新（LINE Works変更）

### 変更内容
- Word: LINE Official → LINE Works への変更理由・比較表・開発工数削減効果を追記
- Excel: 「LINE Works連携設計」シートを追加（比較表・連携機能設計・事前準備タスク更新）
- PowerPoint: スライド14追加（LINE Works採用理由・連携機能詳細）

### 変更理由
- 警備員とのやりとりをLINE Official AccountからLINE Worksに変更するため
- LINE Worksはネイティブチャット機能を持つため開発工数が大幅削減（約3日→0.5日）
- 管理者アカウント制御・既読確認・グループ機能が標準提供される

### 影響範囲
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.docx`
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.xlsx`
- `project/output/GuardSync/20260518_GuardSync_プロジェクト計画書.pptx`

## 2026-05-18 02:00　GuardSync Week 1 初期実装

### 変更内容
- `prisma/schema.prisma` 新規作成（Company/User/Guard/Site/Contract/Partner/Vehicle/Schedule/Attendance/Invoice/DailyPayRequest/ElectronicContract/SecurityReport/LineWorksSettings/Notification/AutoReceipt）
- `server.ts` 新規作成（Express API: 認証/招待/隊員/現場/契約/シフト/出退勤/請求/日払い/電子契約/協力会社/スーパー管理者、cron前日確認）
- `src/` フロントエンド全体を新規作成（React PWA + TailwindCSS）
  - ページ: Login/Register/Dashboard/Guards/Sites/Contracts/Schedule/Attendance/Invoices/DailyPay/EContracts/Partners/SignContract/SuperAdmin
  - コンポーネント: Layout/LoadingSpinner / hooks: useAuth / lib: api/auth
- `prisma/seed.ts` 新規作成（デモデータ）
- 設定ファイル一式: package.json/tsconfig/vite.config.ts/tailwind/postcss/railway.toml

### 変更理由
- GuardSync Week 1実装（DB設計・認証・マスタデータCRUD・管制・請求・日払い・電子契約）

### 影響範囲
- `project/output/GuardSync/` 配下の全ソースファイル
- GitHub: https://github.com/ffconsulting2023-dev/GuardSync

## 2026-05-18 14:00　Week6 自動受付・通知管理・CSVエクスポートUI実装

### 変更内容
- `src/pages/AutoReceiptPage.tsx` 新規作成（FAX/メール/LINE Works受信依頼の確認・承認・却下）
- `src/pages/NotificationsPage.tsx` 新規作成（通知一斉送信・履歴表示）
- `src/App.tsx` に `/auto-receipts`, `/notifications` ルート追加
- `src/components/Layout.tsx` ナビゲーションに「自動受付」「通知管理」追加
- `src/pages/GuardsPage.tsx` CSVエクスポートボタン（⬇ CSV）追加
- `src/pages/InvoicesPage.tsx` CSVエクスポートボタン（⬇ CSV）追加
- `src/pages/DailyPayPage.tsx` CSVエクスポートボタン（⬇ CSV）追加

### 変更理由
- MINプラン Week6 要件（自動受付UI・通知管理・CSVエクスポート）の実装
- FAX/メール/LINE Works経由で受信した配員依頼をオペレーターが確認・承認できるUI
- 管理者が全隊員へ一斉通知を送信できる機能
- 隊員一覧・請求書・日払い申請のCSV一括出力

### 影響範囲
- `src/App.tsx`、`src/components/Layout.tsx`（ルーティング・ナビゲーション変更）
- `src/pages/GuardsPage.tsx`、`src/pages/InvoicesPage.tsx`、`src/pages/DailyPayPage.tsx`（UIボタン追加）

## 2026-05-18 15:00　Week7 メール/FAX受信Webhook・LINE Works接続テスト実装

### 変更内容
- `server.ts` POST /api/inbound/email 追加（SendGrid Inbound ParseからAutoReceipt作成）
- `server.ts` POST /api/inbound/fax 追加（FAX受信stub・OCR連携準備）
- `server.ts` POST /api/settings/line-works/test 追加（接続テストメッセージ送信）
- `src/pages/SettingsPage.tsx` LINE Works接続テストボタン追加

### 変更理由
- FAX・メール経由の配員依頼を自動受付（AutoReceipt）に自動登録するWebhook追加
- LINE Works設定後に管理画面から接続確認できるUI実装
- SendGrid Inbound Parse URL: POST /api/inbound/email
- FAX受信サービスURL: POST /api/inbound/fax

### 影響範囲
- `server.ts`（3エンドポイント追加）
- `src/pages/SettingsPage.tsx`（UI追加）

## 2026-05-18 15:30　PWA設定改善・Railwayデプロイガイド作成

### 変更内容
- `vite.config.ts` PWAアイコン設定を修正（SVGアイコン追加・favicon.ico削除）
- `public/pwa-192x192.png`, `public/pwa-512x512.png` プレースホルダー追加
- `context/Railway環境変数設定ガイド.md` 新規作成（環境変数一覧・Webhook URL・初期ログイン情報）

### 変更理由
- PWAアイコン（PNG）が未作成のためビルド警告が発生していたため
- Railwayデプロイ手順を文書化して環境構築を容易にするため

### 影響範囲
- `vite.config.ts`（PWAアイコン設定変更）
- `public/`（PWAアイコンPNG追加）

## 2026-05-18 16:00　隊員PWAアプリ 警備報告書提出機能実装

### 変更内容
- `src/pages/guard/GuardAppPage.tsx` 報告書タブに実際の提出フォームを追加
  - 現場選択・日付・天候・特記事項・連絡事項を入力して提出
  - 提出済み報告書の一覧表示（承認済み/確認待ちバッジ）

### 変更理由
- 報告書タブが「PCから利用してください」のプレースホルダーだったため
- 隊員がスマートフォンから直接警備報告書を提出できるようにするため

### 影響範囲
- `src/pages/guard/GuardAppPage.tsx`（報告書タブUI追加）

## 2026-05-18 16:30　前日確認手動送信・報告書API改善

### 変更内容
- `server.ts` POST /api/schedules/:id/send-reminder 追加（手動リマインダー送信）
- `src/pages/SchedulePage.tsx` 各シフト行に前日確認メール送信ボタン（📨）追加
- `src/pages/guard/GuardAppPage.tsx` 報告書取得クエリをguardIdフィルタ方式に修正

### 変更理由
- 管制担当者が任意のタイミングで特定の隊員に前日確認を送れるようにするため
- 隊員アプリの報告書一覧でクライアントフィルタ→サーバーフィルタに変更し負荷軽減

### 影響範囲
- `server.ts`（エンドポイント追加）
- `src/pages/SchedulePage.tsx`、`src/pages/guard/GuardAppPage.tsx`

## 2026-05-18 17:00　請求書入金済みマーク機能追加

### 変更内容
- `server.ts` PUT /api/invoices/:id/paid 追加（入金済みステータス更新）
- `src/pages/InvoicesPage.tsx` 「入金済み」ボタン追加（SENT/OVERDUEステータスの請求書に表示）

### 変更理由
- 送付済み請求書の入金確認後にステータスを更新できる機能が未実装だったため

### 影響範囲
- `server.ts`（エンドポイント追加）
- `src/pages/InvoicesPage.tsx`（UIボタン追加）

## 2026-05-18 17:30　出退勤CSVエクスポート機能追加

### 変更内容
- `server.ts` GET /api/export/attendance 追加（出退勤データCSV出力）
- `src/pages/AttendancePage.tsx` CSVエクスポートボタン追加

### 変更理由
- 出退勤実績を月次・日次でCSV出力し、給与計算システムへの連携を可能にするため

### 影響範囲
- `server.ts`（エンドポイント追加）
- `src/pages/AttendancePage.tsx`（UIボタン追加）

## 2026-05-18 18:00　Railway本番デプロイ完了

### 変更内容
- Railwayにguardsyncサービスをデプロイ（サービスID: 87be821d-e8a4-4ce7-be3c-d64fc0f43b84）
- 環境変数設定: JWT_SECRET, DATABASE_URL, NODE_ENV, APP_URL
- Prisma DBスキーマ同期・シードデータ投入完了

### 変更理由
- MINプランの本番リリース準備として、Railway上にアプリケーションを公開

### 影響範囲
- 本番URL: https://guardsync-production.up.railway.app
- 初期ログイン: admin@guardsync.jp / GuardSync2026! (SuperAdmin)
- デモログイン: demo@demo-security.co.jp / demo1234 (ADMIN)

## 2026-05-18 XX:XX　取引先一覧 表示設定・列幅カスタマイズ機能追加

### 変更内容
- `src/pages/ClientsPage.tsx` 全面書き直し
  - `ColDef`型・`loadColumns`/`saveColumns`（localStorage永続化）追加
  - `SettingsPanel`コンポーネント追加（ドラッグ並び替え・チェック表示切替・+/-幅調整）
  - テーブルヘッダー右端ドラッグによるリアルタイム列幅変更
  - `table-layout: fixed` + `<colgroup>`で列幅を正確制御
  - 設定は`guardSync_clientColumns_v2`キーでlocalStorageに保存

### 変更理由
- 取引先一覧の列の表示/非表示・並び順・幅をユーザーが自由に設定できるようにするため

### 影響範囲
- `src/pages/ClientsPage.tsx`のみ

## 2026-05-18 XX:XX　取引先管理フル機能実装（Excel仕様準拠）

### 変更内容
- `prisma/schema.prisma` Client に大量フィールド追加（代表役職/FAX/業種/関係性/経理担当/subContacts/住所構造化/請求先住所/書類フラグ/振込先/契約単価）
- `server.ts` 取引先POST/PUT を全フィールド対応・コード形式をC00001に変更
- `src/pages/ClientsPage.tsx` テーブル列を仕様通りに変更（C00001/都道府県/市区町村/FAX/担当者1/連絡先1/現場ボタン）
- `src/pages/ClientDetailPage.tsx` 6セクション全対応（①基本情報・②担当者×4・③会社住所・④請求先住所・④書類・⑤振込先・⑥契約単価）
- Railway DB: `prisma db push`でスキーマ同期済み

### 変更理由
- Excelベースの取引先管理仕様に合わせた全フィールド対応
- 複数担当者・構造化住所・振込先・契約単価の登録機能追加

### 影響範囲
- `prisma/schema.prisma`（Client大幅更新）
- `server.ts`（取引先API更新）
- `src/pages/ClientsPage.tsx`（テーブル列更新）
- `src/pages/ClientDetailPage.tsx`（全面刷新）

## 2026-05-18 XX:XX　取引先管理UI全面刷新（Salesforce参考）

### 変更内容
- `prisma/schema.prisma` Client に clientCode/category/website/invoiceRegistrationNumber を追加
- `prisma/schema.prisma` ClientLog モデル新規追加（logType/content/createdByName）
- `server.ts` 取引先API全面更新（clientCode自動生成・category/website/インボイス対応・ログAPI追加）
- `server.ts` GET /api/clients/:id 追加（sites・logs含む詳細取得）
- `server.ts` GET/POST /api/clients/:id/logs 追加（連絡ログCRUD）
- `src/pages/ClientsPage.tsx` テーブルUI・区分フィルタタブ・3点メニュー・取引先コード表示に全面刷新
- `src/pages/ClientDetailPage.tsx` 新規作成（詳細ページ・タブ・右サイドバー・連絡ログ）
- `src/App.tsx` `/clients/:id` ルート追加
- Railway DB: `prisma db push`でスキーマ同期済み

### 変更理由
- Salesforceスタイルの一覧テーブル + 詳細ページUIに変更するため
- 取引先コード自動生成・区分管理・インボイス番号・活動ログ機能を追加するため

### 影響範囲
- `prisma/schema.prisma`（Client更新・ClientLog追加）
- `server.ts`（取引先・ログAPI更新・追加）
- `src/pages/ClientsPage.tsx`（全面刷新）
- `src/pages/ClientDetailPage.tsx`（新規）
- `src/App.tsx`（ルート追加）

## 2026-05-18 XX:XX　取引先管理機能追加・現場管理連動

### 変更内容
- `prisma/schema.prisma` `Client`モデル新規追加（name/nameKana/contactName/phone/email/address/notes）
- `prisma/schema.prisma` `Site`モデルに`clientId`（任意FK）を追加
- `prisma/schema.prisma` `Company`モデルに`clients`リレーション追加
- `server.ts` 取引先APIを追加（GET/POST/PUT/DELETE `/api/clients`）
- `server.ts` 現場APIを更新（`clientId`対応・`client`インクルード）
- `src/pages/ClientsPage.tsx` 新規作成（取引先CRUD・現場件数表示）
- `src/pages/SitesPage.tsx` 取引先ドロップダウン選択・連動自動入力に更新
- `src/App.tsx` `/clients/*`ルート追加
- `src/components/Layout.tsx` ナビゲーションに「取引先管理」追加
- Railway DB: `prisma db push`でスキーマ同期済み

### 変更理由
- 取引先（発注元）を独立したマスタとして管理するため
- 現場に取引先を紐付け、担当者・連絡先を一元管理するため
- 既存の`clientName`/`clientPhone`手入力との後方互換性を維持

### 影響範囲
- `prisma/schema.prisma`（Client追加・Site更新）
- `server.ts`（取引先API追加・現場API更新）
- `src/pages/ClientsPage.tsx`（新規）
- `src/pages/SitesPage.tsx`（更新）
- `src/App.tsx`、`src/components/Layout.tsx`（ルーティング・ナビゲーション）

## 2026-05-18 10:00　LINE Works 個人メッセージ方式に変更

### 変更内容
- `prisma/schema.prisma` LineWorksSettings に `botSecret` フィールドを追加
- `server.ts` 認証方式をOAuth（Client ID/Secret）からBot ID + Bot Secret（client_credentials）に変更
- `server.ts` メッセージ送信をチャンネル宛（/channels/）から個人宛（/users/）に変更
- `server.ts` 接続テストAPIに `testUserId` パラメータを追加
- `server.ts` 設定APIで `channelId` を任意項目に変更、`botSecret` を受け付けるよう更新
- `src/pages/SettingsPage.tsx` Bot Secret入力欄・テスト送信先メンバーID入力欄を追加
- `src/pages/GuardsPage.tsx` LINE Works メンバーID入力欄を追加
- `.env` に `LINE_WORKS_BOT_ID`・`LINE_WORKS_BOT_SECRET` を追加
- RailwayのDB（schema）を `prisma db push` で更新

### 変更理由
- 固定グループチャンネルではなく隊員個人のLINE Worksアカウントに直接メッセージ送信する方式に変更
- Bot ID: 12266640（株式会社MVV / mvv）を利用

### 影響範囲
- `prisma/schema.prisma`（LineWorksSettings.botSecret追加）
- `server.ts`（getLineWorksToken・sendLineWorksMessage・設定API・接続テストAPI変更）
- `src/pages/SettingsPage.tsx`（UI変更）
- `src/pages/GuardsPage.tsx`（lineWorksId入力欄追加）

## 2026-05-18 19:00　セキュリティ脆弱性修正（全19項目）

### 変更内容

**Critical（C-1〜C-4）:**
- `.env.example` 新規作成（環境変数テンプレート、実際の値は含まない）
- JWT_SECRET 起動時バリデーション追加（32文字未満で起動拒否）
- Webhookエンドポイント（`/api/inbound/email`, `/api/inbound/fax`）にシークレットトークン認証追加
- FAX/メール受信のテナント分離改善（`INBOUND_COMPANY_CODE`環境変数による会社特定）

**High（H-1〜H-7）:**
- 隊員APIの機密フィールド（bankAccount, lineWorksId）を権限別にフィルタリング
- 取引先API（POST/PUT）にzodバリデーションスキーマ追加
- CORS設定を明示的なオリジンリストに変更（本番: APP_URL、開発: localhost）
- Content Security Policy（CSP）を有効化（Helmetで設定）
- Update系クエリにcompanyId条件を追加（TOCTOU対策、guard/client/site/schedule/vehicle/invoice）
- パスワード強度要件を強化（8文字以上、大文字・小文字・数字を必須化）
- LINE Works機密情報（botSecret）をAES-256-CBCで暗号化保存

**Medium（M-1〜M-9）:**
- レート制限を強化（API: 200→100回/15分、ログイン: 20→10回/15分、Webhook専用設定追加）
- スーパー管理者招待APIのcompanyId必須チェック追加
- CSVインジェクション対策（全CSV出力箇所にescapeCsvCell適用）
- グローバルエラーハンドラー・404ハンドラー追加
- フロントエンドのスーパー管理者ルートガード追加（isSuperAdminチェック）
- Service WorkerのAPIキャッシュをNetworkOnlyに変更（機密データ保護）

**Low（L-2, L-3, L-6）:**
- Prismaログレベルをwarn/errorに制限
- JWTトークン有効期限を8時間→4時間に短縮
- メールテンプレートのHTMLエスケープ関数追加（全メール送信箇所に適用）

### 変更理由
- セキュリティ監査で検出された脆弱性を優先順位に従って修正するため
- 本番運用に向けた認証・認可・データ保護の強化

### 影響範囲
- `server.ts`（メイン修正対象: 認証・バリデーション・暗号化・CSV・エラーハンドリング）
- `src/App.tsx`（スーパー管理者ルートガード追加）
- `vite.config.ts`（Service WorkerのAPIキャッシュ無効化）
- `.env.example`（新規作成）
- **注意**: JWT_SECRET起動時バリデーション追加により、`.env`のJWT_SECRETを32文字以上に変更する必要あり（`openssl rand -hex 32`で生成推奨）

## 2026-05-20 10:00　パスワード表示切替＆パスワードリセット機能追加

### 変更内容
- `LoginPage.tsx`: パスワード入力欄に表示/非表示トグルボタン追加、「パスワードをお忘れの方」リンク追加
- `ForgotPasswordPage.tsx`: 新規作成 - メールアドレス入力→リセット用URL送信フォーム
- `ResetPasswordPage.tsx`: 新規作成 - トークン検証→新パスワード入力・確認フォーム（表示切替付き）
- `server.ts`: `POST /api/auth/forgot-password`・`POST /api/auth/reset-password` APIエンドポイント追加
- `prisma/schema.prisma`: `PasswordResetToken` モデル追加（token, userId, expiresAt, usedAt）
- `App.tsx`: `/forgot-password`・`/reset-password` ルート追加
- Railway DB: `db push` でマイグレーション済み

### 変更理由
- ユーザーからの要望：パスワード表示ボタンとパスワードリセットフロー（メール認証）の実装

### 影響範囲
- 認証関連ページ全般（LoginPage, ForgotPasswordPage, ResetPasswordPage）
- server.ts の認証エンドポイント
- Prisma スキーマ・Railway DB

## 2026-05-19 15:00　エクセル仕様書に基づくスキーマ・UI大規模更新

### 変更内容
- Guard モデル: 住所構造化・最寄駅・クラス・スキル・NG設定・評価・給与体系・勤務単価・手当・社会保険・家族構成・緊急連絡先・書類チェック等50+フィールド追加
- Site モデル: 現場コード・必要隊員数・資格者要件・集合時間・開始/終了予定・集合場所・注意点を追加
- Schedule モデル: shiftType・assemblyTime・sentAt追加
- Attendance モデル: isHoliday・早出/遅出残業(分)・旅費・その他手当を追加
- 新モデル: ShiftSurvey, ShiftSurveyResponse（シフトアンケート）
- 新モデル: Payroll（給与明細・勤怠サマリ・支給・控除・差引支給額）
- 新モデル: SubcontractorPayment（支払管理・協力会社向け）
- 新enum: PayrollStatus, SubPaymentStatus
- GuardsPage.tsx: 7セクションタブ形式フォーム（基本情報/現住所/緊急連絡先/NG設定/給与・社保/書類/振込先）
- SitesPage.tsx: 現場フォームに現場コード・必要隊員数・資格者要件・集合時間・注意点等を追加
- 新ページ: PayrollPage.tsx（給与管理 - 月切替・隊員展開・明細編集・自動計算・ステータス管理）
- 新ページ: SubcontractorPaymentPage.tsx（支払管理 - 月切替・タブフィルタ・明細展開・ステータス遷移）
- server.ts: Guard/SiteのZodスキーマ・API拡張、ShiftSurvey/Payroll/SubcontractorPayment新APIエンドポイント追加
- App.tsx: /payroll, /subcontractor-payments ルート追加
- Layout.tsx: ナビゲーションに「給与管理」「支払管理」追加
- Railway DB: prisma db push 実施済み

### 変更理由
- 警備管理システム.xlsx（システム骨子・各UI仕様）に基づく仕様反映
- 隊員の詳細情報管理・給与計算・支払管理機能の実装

### 影響範囲
- `prisma/schema.prisma`（Guard/Site/Schedule/Attendance拡張、ShiftSurvey/Payroll/SubcontractorPayment新規）
- `server.ts`（Guard/Site API拡張、ShiftSurvey/Payroll/SubcontractorPayment API新規）
- `src/pages/GuardsPage.tsx`（7セクションフォームに全面刷新）
- `src/pages/SitesPage.tsx`（新フィールド対応）
- `src/pages/PayrollPage.tsx`（新規）
- `src/pages/SubcontractorPaymentPage.tsx`（新規）
- `src/App.tsx`（ルート追加）
- `src/components/Layout.tsx`（ナビゲーション追加）
